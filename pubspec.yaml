name: crypto_thing
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  fl_chart: ^0.68.0
  flutter:
    sdk: flutter
  http: ^1.2.1
  intl: ^0.19.0
  logging: ^1.2.0
  provider: ^6.1.2
  syncfusion_flutter_charts: ^25.2.6
  url_launcher: ^6.2.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/cheatsheet.jpg
